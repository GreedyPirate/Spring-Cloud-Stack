server:
  port: 8897
  servlet:
    context-path: /consumer-api
spring:
  application:
    name: eureka-consumer-feign

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8000/eureka/
#    healthcheck:
#      enabled: true 版本变化？
feign:
  httpclient:
    enable: true
debug: true